Added a ``mpi_mode`` argument to :py:func:`opencosmo.open`. The default, ``"spatial"``, preserves the existing behavior where every MPI rank opens every file and reads a spatial sub-partition. The new ``"redshift"`` mode splits the redshift-ordered steps into contiguous chunks of roughly-equal data volume and gives each rank one chunk, so each file is opened by exactly one rank and each rank owns a continuous redshift range. This eliminates the redundant filesystem metadata traffic that made spatial splitting pathological for many-file lightcones: only rank 0 reads every header (to build the plan), and each rank then constructs targets solely for its assigned files. Ranks that receive no files still hold a full-schema, zero-length dataset, so ``select``/``filter`` and scalar reductions behave identically on every rank. Nested (Diffsky step-then-type) lightcones automatically fall back to spatial distribution.
