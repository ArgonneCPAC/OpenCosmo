Added a ``mpi_mode`` argument to :py:func:`opencosmo.open`. The default, ``"spatial"``, preserves the existing behavior where every MPI rank opens every file and reads a spatial sub-partition. The new ``"redshift"`` mode distributes lightcone files across ranks by data volume so each file is opened by exactly one rank, eliminating the redundant filesystem metadata traffic that made spatial splitting pathological for many-file lightcones. Ranks that receive no files still hold a full-schema, zero-length dataset, so ``select``/``filter`` and scalar reductions behave identically on every rank. Nested (Diffsky step-then-type) lightcones automatically fall back to spatial distribution.
